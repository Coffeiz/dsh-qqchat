# Changelog

本文件记录当前分支相对 `main` 的用户可见变化。内部同步探针、临时文件和实现重排已合并到对应的维护项中。

## Unreleased

### 架构与运行时

- 将 QQ Chat 插件迁移到 TypeScript，补充 Host、Client、构建产物和类型声明的独立配置。
- 将 QQ 聊天接入 DSH 原生 Workspace，使用原生会话侧栏和会话路由，不再在插件中重复维护一套聊天列表。
- 将触发 Agent 的 QQ 消息按原生 user turn 写入 DSH 会话，避免 SQLite 转录与会话转录重复、当前消息被重复注入历史的问题。
- 保留 QQ Gateway、授权、Agent bridge、RPC、数据库和运行时的清晰边界，并补充日志查看后端。
- 增加 TypeScript 类型检查、Client/Host 构建配置，以及 CI 中的插件检查流程。

### QQ 会话与消息

- 收紧群聊 Slash Command 权限：普通群成员只能使用查询类命令，Session、模型和生成控制命令仅限 Owner。
- 修复同一群聊或私聊中 WebSocket DISPATCH 处理并发导致的消息乱序；不同聊天仍可并行处理。
- 让群级 `enabled`、`requiresAt` 和 `readEnabled` 设置真正参与消息接收和 Agent 触发判断。
- QQ 群聊和私聊使用 DSH 原生会话展示，支持从 QQ 会话进入对应的 DSH 工作区。
- QQ 消息进入 Agent 上下文前会排除当前待回复消息，避免模型把正在处理的消息当作历史消息。
- 群成员详情可以从 QQ 会话中查看，包括成员资料、行为模式、摘要和长期记忆。
- 增加 QQ Chat 日志查看入口，覆盖 Gateway、授权、Agent bridge 和消息发送相关日志。
- 修复 QQ 消息触发 Agent 时的重复转录和 SQLite 历史回放问题。
- 增加 QQ @消息的显示名解析，在 DSH 消息和记忆上下文中优先显示用户名或群内称呼，而不是裸用户 ID。
- 群聊和私聊支持分别设置消息兼容格式；群聊默认使用纯文本兼容格式，私聊沿用插件配置的默认格式。
- 私聊接入 QQ 官方 C2C `stream_messages` 流式方案，使用全文替换、节流和串行更新；群聊继续使用普通发送。
- 增加私聊流式回复设置开关；纯文本兼容模式会自动关闭流式，流式失败且尚未发送内容时自动回退到普通消息。
- 修复流式回复 `msg_seq` 重复和后续帧未及时携带 `stream_msg_id` 导致的 QQ `40054005` 去重错误。
- 修复群消息附件通过同一成员私聊 Session 越权读取的问题，并修正媒体清理任务使附件按 7 天 TTL 到期。

### 记忆与反思

- 群聊记忆支持 profile、summary、daily 和 memory 主档，并将 daily 按日期标题归档。
- 群成员记忆支持 profile、pattern、summary、daily 和 memory，并记录群内称呼、历史昵称等身份信息。
- 私聊接入群成员记忆范围，支持 profile、pattern、summary、daily 和 memory 的异步沉淀。
- 群聊和私聊的记忆反思均采用异步调度，不阻塞 QQ 主消息回复。
- 增加群聊和私聊的 daily 压缩机制：成员记录达到 100 条、群记录达到 1000 条时，保留最近记录并将旧记录整理进长期 memory。
- 记忆压缩增加日期保留校验和失败保护；压缩失败或结果不完整时不会删除原始 daily 记录。
- 首次静默创建会话后也会正确安排记忆任务，避免首条消息没有进入反思队列。
- 当前 QQ 消息不会被错误写入 recent-history 快照，减少重复记忆和上下文污染。
- 增强记忆反思 JSON 解析，兼容模型返回的 JSON 代码块和前后附带说明；无效结果仍会跳过本批处理，不覆盖已有记忆。
- 修复记忆快照恢复读取错误的 Session event 数据结构，并为群聊和私聊记忆反思增加 scope 级并发锁。

### 设置与界面

- 拆分 QQ Chat Client、设置页和 Workspace UI，统一复用 DSH 原生 UI primitives。
- 群聊接收方式改为原生按钮组选项，支持自动回应、@回复和静默记录。
- Owner 使用 stable sender ID 配置，并提供基于已知群成员的选择入口。
- 工具权限、消息格式、日志和记忆详情等设置接入 DSH 原生控件与弹窗。
- 设置、日志和记忆弹窗增加合适的宽度、内容换行和内部滚动限制，长内容不会撑出浏览器窗口。
- 调整设置页布局与原生 DSH 视觉风格，修复消息接收配置区域被压缩为竖向长条的问题。
- 重新整理中英文 README 和架构文档，使其与原生 DSH 会话架构保持一致。

### 工程维护

- 删除未使用的 sidebar client 依赖和旧 JavaScript 实现，保留 TypeScript 源码作为唯一实现入口。
- 更新构建脚本以生成 Client bundle，并通过包装脚本提供 DSH 插件加载入口。
- CI 不再尝试使用不存在的 lockfile 读取 npm 缓存，避免无 lockfile 项目的构建误报。
- 补充开发、架构、记忆、安全、变更记录和协作约定文档。
- 新增 QQ 私聊流式传输设计文档，记录官方协议、队列生命周期、降级策略和故障复盘。
